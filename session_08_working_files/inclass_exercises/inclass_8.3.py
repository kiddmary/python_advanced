# 8.3:  DataFrame from python struct.  Use this 3x3 list of lists to build a DataFrame using the
# pd.DataFrame() constructor.  Add a keyword argument columns=[] to the DataFrame to assign column
# labels, and index=[] to assign row labels.  Refer to the DataFrame to see it.

import pandas as pd

# Sample data in the form of a list of lists
data = [
    [1, 2.5, 'apple'],
    [2, 3.0, 'banana'],
    [3, 4.2, 'orange']
]





